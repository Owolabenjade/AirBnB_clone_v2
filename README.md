# AirBnB Clone V2

This repository contains the implementation of a Flask web application for the AirBnB clone project.

## Files

- `0-hello_route.py`: The main file that starts the Flask web application.
- `__init__.py`: Initialization file for the `web_flask` module.

## Usage

To start the Flask web application, run the following command:

5. Ensure the code follows PEP 8 style guidelines and includes necessary documentation:

- Run `pycodestyle 0-hello_route.py` to check for PEP 8 compliance.
- Add appropriate docstrings to the module and function.

6. To start the Flask application, run:

```bash
python3 -m web_flask.0-hello_route

